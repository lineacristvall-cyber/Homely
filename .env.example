# Local mode needs no cloud credentials. Never commit .env.local.
HOMELY_STORAGE=local
# Optional AI features; configure a server-side key only when needed.
OPENAI_API_KEY=
# Cloud mode requires the allowlisted project and schema; see backend/cloud/README.md.
SUPABASE_PUBLISHABLE_KEY=
